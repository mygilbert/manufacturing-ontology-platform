# ============================================================
# Manufacturing Ontology Platform - 환경 변수
# ============================================================
# 사용법: 이 파일을 .env로 복사하여 값을 수정하세요
# cp .env.example .env
# ============================================================

# PostgreSQL + Apache AGE (온톨로지 DB)
POSTGRES_USER=ontology
POSTGRES_PASSWORD=ontology123
POSTGRES_DB=manufacturing

# TimescaleDB (시계열 DB)
TIMESCALE_USER=timescale
TIMESCALE_PASSWORD=timescale123
TIMESCALE_DB=measurements

# Redis
REDIS_PASSWORD=redis123

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# API Server
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true

# Frontend
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000

# Legacy DB 연결 (Debezium CDC용)
# FDC Oracle
FDC_ORACLE_HOST=
FDC_ORACLE_PORT=1521
FDC_ORACLE_SID=
FDC_ORACLE_USER=
FDC_ORACLE_PASSWORD=

# SPC SQL Server
SPC_SQLSERVER_HOST=
SPC_SQLSERVER_PORT=1433
SPC_SQLSERVER_DB=
SPC_SQLSERVER_USER=
SPC_SQLSERVER_PASSWORD=

# MES Oracle
MES_ORACLE_HOST=
MES_ORACLE_PORT=1521
MES_ORACLE_SID=
MES_ORACLE_USER=
MES_ORACLE_PASSWORD=
